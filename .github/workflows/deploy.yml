name: Deploy Laravel to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy Laravel
        run: |
          cd /var/www/laravel-api
          ./deploy.sh
